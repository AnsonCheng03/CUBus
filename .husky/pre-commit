#!/bin/sh

set -e

staged_files="$(git diff --cached --name-only --diff-filter=ACMR)"

if [ -z "$staged_files" ]; then
  exit 0
fi

echo "$staged_files" | grep -Eq '^(mobile/|src/shared-core/)' || exit 0

echo "Running mobile checks for staged changes..."
npm run mobile:check
